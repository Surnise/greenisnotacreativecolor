package command.abs;

import command.ICommand;
import wrapper.Wrapper;

public class AskDoctorCommand //implements ICommand
{
    //@Override
    public String execute(Wrapper wrapper) {
        return null;
    }
}
